<!DOCTYPE html>
<html>
<head>
  <title>Expense Form</title>
  <style>
  /* Style for the task form container */
  .task-form {
    max-width: 500px;
    margin: 0 auto;
    padding: 20px;
    background-color: #f9f9f9;
    border-radius: 10px;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
  }
  
  /* Style for each form group */
  .form-group {
    margin-bottom: 15px;
  }
  
  /* Style for the labels */
  .form-label {
    display: block;
    font-weight: bold;
    margin-bottom: 5px;
    font-size: 14px;
  }
  
  /* Style for the input fields */
  .form-input {
    width: 100%;
    padding: 8px;
    font-size: 14px;
    border: 1px solid #ccc;
    border-radius: 5px;
  }
  
  /* Style for the error messages */
  .error-message {
    color: red;
    font-size: 12px;
    margin-top: 5px;
  }
  
  /* Style for the submit button */
  .form-submit {
    background-color: #28a745;
    color: white;
    padding: 10px 20px;
    border: none;
    border-radius: 5px;
    font-size: 14px;
    cursor: pointer;
  }
  
  /* Hover effect for the submit button */
  .form-submit:hover {
    background-color: #218838;
  }
  
  </style>
</head>
<body>
<h1 style="text-align: center;">Expense Tracker System</h1>
<%= form_with model: @expense,html: { class: 'task-form' } do |form| %>
  <div class="form-group">
    <%= form.label :title, class: 'form-label' %>
    <%= form.text_field :title, class: 'form-input' %>
    <% expense.errors.full_messages_for(:title).each do |message|%>
      <div class="error-message"><%= message %></div>
    <% end %>  
   </div>
  <br>
  <div class="form-group">
    <%= form.label :date, class: 'form-label' %>
    <%= form.text_field :date, class: 'form-input' %>
    <% expense.errors.full_messages_for(:status).each do |message| %>
      <div class="error-message"><%= message %></div>
    <% end %> 
  </div>
   <br>
  <div class="form-group">
    <%= form.label :description, class: 'form-label' %>
    <%= form.text_area :description, class: 'form-input' %>
    <% expense.errors.full_messages_for(:description).each do |message| %>
      <div class="error-message"><%= message %></div>
    <% end %>    
  </div>
  <br>
  <div class="form-group">
    <%= form.label :user_id, class: 'form-label' %>
    <%= form.text_field :user_id, class: 'form-input' %>
    <% expense.errors.full_messages_for(:user_id).each do |message| %>
      <div class="error-message"><%= message %></div>
    <% end %> 
  </div>
  <br>
  <div class="form-group">
    <%= form.submit 'submit', class: 'form-submit' %>
  </div>
<% end %>
</body>
</html>